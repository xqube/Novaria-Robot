export const config = {
    BOT_TOKEN : "token",
    MONGO_URL : 'Mongo-DB URL'
}

//for uploading the group picture to cloudinary using the /register
export const cloudinarykeys = {
    cloud_name: '',
    api_key: '',
    api_secret: ''
}